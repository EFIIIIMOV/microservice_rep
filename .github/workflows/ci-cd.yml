#name: CI/CD pipline
#
#on:
#  push:
#    branches: [ "master" ]
#
#jobs:
#  build-and-push-to-dh:
#    name: Build and push to DockerHub
#    #needs: [ test-app-order ]
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#
#      - name: Build docker app_delivery image
#        run: |
#          docker build ./app_order -f ./app_order/Dockerfile --tag ${{ secrets.DOCKER_USERNAME }}/app_order:latest
#
#      - name: Login to DockerHub
#        uses: docker/login-action@v3.0.0
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#
#      - name: Build and push app_order service image
#        run: |
#          docker push ${{ secrets.DOCKER_USERNAME }}/app_order:latest

name: CI/CD pipline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push-to-dh:
    name: Build and push to DockerHub
    #needs: [ test-app-order ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build docker app_delivery image
        run: |
          docker build ./app_order -f ./app_order/Dockerfile --tag app_order:latest

      - name: Login to DockerHub
        uses: docker/login-action@v3.0.0
        with:
          username: EFIIIIMOV
          password: IK06f10FIhttp0f


      - name: Build and push app_order service image
        run: |
          docker push efiiiimov/app_order:latest